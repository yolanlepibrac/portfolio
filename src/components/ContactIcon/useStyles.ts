import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  iconButton: {
    cursor: "pointer",
    height: 40,
    borderStyle: "solid",
    borderWidth: 1,
    borderRadius: 5,
  },
}));
