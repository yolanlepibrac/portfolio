import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  iconContainer: {
    padding: 1,
    borderRadius: 5,
  },
  iconButton: {
    cursor: "pointer",
    height: 40,
    borderRadius: 4,
    backgroundColor: "white",
  },
}));
